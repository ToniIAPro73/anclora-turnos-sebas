# Database for Vercel Functions + Neon
DATABASE_URL=postgresql://user:password@host/db?sslmode=require
DATABASE_URL_UNPOOLED=postgresql://user:password@host-unpooled/db?sslmode=require
POSTGRES_URL=postgresql://user:password@host/db?sslmode=require
POSTGRES_URL_NON_POOLING=postgresql://user:password@host-unpooled/db?sslmode=require
VITE_TURNO_APP_API_KEY=tu_api_key_publica_del_endpoint

# Public extraction endpoint from turno-app
# The current frontend calls the public Manus endpoint directly, so no local Forge key is required.
#
# If you want to keep the old local proxy flow for debugging, configure it separately.
